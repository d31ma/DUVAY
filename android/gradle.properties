# AndroidX is required by Compose and Material 3.
android.useAndroidX=true
# The Android Gradle Plugin's default heap is not enough for the Compose
# compiler plugin on a module this size.
org.gradle.jvmargs=-Xmx3g -XX:MaxMetaspaceSize=1g
